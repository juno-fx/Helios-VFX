services:
  helios:
    image: junoinnovations/helios:unstable-jammy
    hostname: "helios"
    build:
      context: .
      dockerfile: Dockerfile
    container_name: helios
#    pre_stop:
#      - command: /etc/helios/shutdown.d/custom.sh
#    volumes:
#      - /dev/dri:/dev/dri
#      - .out/:/mnt/
#      - .home:/home/
    environment:
      - USER=helios
      - UID=5040
      - SUDO=true
#      - DISABLE_VGL=1
#      - DESKTOP_FILES="/desktops/*.desktop:/tmp/*.desktop"
#      - IDLE_TIME=1
#      - PASSWORD=password
#      - PREFIX=/custom/prefix
      - REMOTE_PROTOCOL=${REMOTE_PROTOCOL:-selkies}  # selkies|dcv
#      - DCV_DIRECT_PORT=8443  # password-authenticated native-client endpoint (needs PASSWORD)
#      - DCV_LICENSE_FILE=/path/to/license.lic  # DCV license for non-EC2 usage
    ports:
      - "3000:3000"
      - "3001:3001"
      - "8443:8443"      # DCV direct connect (TCP)
      - "8443:8443/udp"  # DCV direct connect (QUIC)
